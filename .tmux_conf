# remap prefix to Control + a
#set -g prefix C-a
# bind 'C-a C-a' to type 'C-a'
#bind C-a send-prefix
#unbind C-b

# Mouse support - set to on if you want to use the mouse
#* setw -g mode-mouse off
#* set -g mouse-select-pane off
#* set -g mouse-resize-pane off
#* set -g mouse-select-window off

# Set the default terminal mode to 256color mode
#set -g default-terminal "screen-256color"

# enable activity alerts
#setw -g monitor-activity on
#set -g visual-activity on

# Center the window list
#set -g status-justify centre

# Maximize and restore a pane
#unbind Up bind Up new-window -d -n tmp \; swap-pane -s tmp.1 \;
#select-window -t tmp
#unbind Down
#bind Down last-window \; swap-pane -s tmp.1 \; kill-window -t tmp

#C-a ?
#C-a M-n        next window with a bell, activity or
#               content alert
#C-a M-p        previous such window

#C-a ,          rename the current window
#C-a &          kill the current window

# Look at resize-pane (C-a : resize-pane [ -D | -U | -L | -R ] n
# C-a [ to get copy mode, then "enter"

# unbind C-b
# set -g prefix M-a
# Use meta-arrows to navigate
# bind -n M-Left select-pane -L
# bind -n M-Right select-pane -R
# bind -n M-Up select-pane -U
# bind -n M-Down select-pane -D

# visibility
# set-window-option -g window-status-current-bg yellow

# notifications
# setw -g monitor-activity on
# set -g visual-activity on

# mouse stuff
# set-option -g mouse-select-pane on

# Example set up:
# selectp -t 0 # select the first (0) pane
# splitw -h -p 50 # split it into two halves

# selectp -t 1 # select the new, second (1) pane
# splitw -v -p 50 # split it into two halves
# selectp -t 0 # go back to the first pane
# splitw -h -p 49 'vim' # split current pane and run 'vim' command inside the new one

# or put in a file: (and use C-b D to invoke)
# bind D source-file ~/.tmux/dev

